<!DOCTYPE html>
<html>
<head>
<meta charset='utf-8' />
<link href='core_main.css' rel='stylesheet' />
<link href='daygrid_main.css' rel='stylesheet' />
<script src='core_main.js'></script>
<script src='inter_main.js'></script>
<script src='daygrid_main.js'></script>
<body>
  <div style = "float: right">
    <a href ="/home" ><img src="https://image.flaticon.com/icons/svg/2948/2948025.svg" width = "30" class="home"></a>
    <a href='/logout' > <img src="https://image.flaticon.com/icons/svg/126/126467.svg" width = "30" class="home logout"></a>
  </div>
  <h1><%=userid%>의 영양 캘린더</h1>
  <div id='calendar'></div>
</body>
<script>
  var tevents = [];
  <% for(var i=0; i<events.length; i++){%>
    tevents[<%=i%>] = {
      "title" : "<%=events[i].title%>",
      "start" : "<%=events[i].start%>",
      "url" : "<%=events[i].url%>"
    }
  <% } %>;

  console.log(tevents);
   document.addEventListener('DOMContentLoaded', function() {
    var calendarEl = document.getElementById('calendar');
    var calendar = new FullCalendar.Calendar(calendarEl, {
      plugins: [ 'interaction', 'dayGrid' ],
      defaultDate: '2020-06-01',
      editable: true,
      eventLimit: true, // allow "more" link when too many events
      events: tevents
    });
    calendar.render();
  });

</script>

<style>

  body {
    margin: 40px 10px;
    padding: 0;
    font-family: Arial, Helvetica Neue, Helvetica, sans-serif;
    font-size: 14px;
  }

  #calendar {
    max-width: 900px;
    margin: 0 auto;
  }

</style>
</head>
</html>
